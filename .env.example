# Django Configuration
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=https://yourapp.fly.dev

# Database (leave empty for SQLite, or use PostgreSQL URL for production)
# DATABASE_URL=postgresql://user:password@localhost/dbname

# Web Push Notifications (VAPID)
# Generate keys with: uv run python manage.py generate_vapid_keys
# IMPORTANT: VAPID_PRIVATE_KEY must be on a single line with \n as literal characters
# Example: VAPID_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIGH...\n-----END PRIVATE KEY-----"
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_ADMIN_EMAIL=admin@example.com
